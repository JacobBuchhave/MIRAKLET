{
  "name": "your-circle-sync-project",
  "version": "1.0.0",
  "description": "Syncs Circle members to Webflow CMS using Vercel scheduled functions.",
  "main": "api/syncMembers.js", // Corrected name
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "node api/syncMembers.js" // Corrected name
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.0.0" // Corrected to a valid version
  }
}